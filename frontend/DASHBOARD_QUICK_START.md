# Dashboard Quick Start Guide\n\n## 🚀 Getting Started\n\n### Basic Setup\n\n```typescript\nimport { DashboardMain } from '@/components/dashboard';\n\nfunction App() {\n  return <DashboardMain />;\n}\n```\n\n## 📦 Common Imports\n\n```typescript\n// Main dashboard\nimport { DashboardMain } from '@/components/dashboard';\n\n// Pages\nimport {\n  DashboardOverview,\n  TransactionsPage,\n  TransfersPage,\n  VirtualCardsPage,\n} from '@/components/dashboard';\n\n// Shared components\nimport {\n  StatusBadge,\n  StatCard,\n  TransactionItem,\n  FormInput,\n  FormSelect,\n  FormTextarea,\n} from '@/components/dashboard';\n\n// Layout\nimport { Sidebar, DashboardHeader } from '@/components/dashboard';\n```\n\n## 🎯 Component Quick Reference\n\n### StatusBadge\nStatus indicators for transactions and actions.\n\n```typescript\n<StatusBadge status=\"success\" label=\"Completed\" size=\"md\" />\n<StatusBadge status=\"pending\" label=\"Processing\" size=\"sm\" />\n<StatusBadge status=\"error\" label=\"Failed\" size=\"lg\" />\n```\n\n**Props**:\n- `status`: 'success' | 'pending' | 'warning' | 'error'\n- `label`: string\n- `size`: 'sm' | 'md' | 'lg' (default: 'md')\n\n### StatCard\nDisplays statistics with trends.\n\n```typescript\nimport { DollarSign, TrendingUp } from 'lucide-react';\n\n<StatCard\n  label=\"Total Balance\"\n  value=\"$25,430.00\"\n  change={12.5}\n  trend=\"up\"\n  icon={<DollarSign size={24} />}\n/>\n```\n\n**Props**:\n- `label`: string\n- `value`: string | number\n- `change`: number (optional)\n- `trend`: 'up' | 'down' | 'neutral' (default: 'neutral')\n- `icon`: React.ReactNode (optional)\n\n### TransactionItem\nReusable transaction list item.\n\n```typescript\nimport { ArrowUpRight } from 'lucide-react';\n\n<TransactionItem\n  icon={<ArrowUpRight />}\n  title=\"Money Transfer\"\n  subtitle=\"To John Doe\"\n  amount=\"+$500.00\"\n  date=\"Today, 2:30 PM\"\n  status=\"success\"\n  amountColor=\"positive\"\n  onClick={() => console.log('clicked')}\n/>\n```\n\n**Props**:\n- `icon`: React.ReactNode\n- `title`: string\n- `subtitle`: string (optional)\n- `amount`: string\n- `date`: string\n- `status`: 'success' | 'pending' | 'warning' | 'error'\n- `amountColor`: 'default' | 'positive' | 'negative' (default: 'default')\n- `onClick`: () => void (optional)\n\n### FormInput\nText input with validation.\n\n```typescript\nimport { Mail } from 'lucide-react';\n\n<FormInput\n  label=\"Email\"\n  type=\"email\"\n  placeholder=\"you@example.com\"\n  icon={<Mail size={18} />}\n  error={errors.email}\n  helperText=\"We'll never share your email\"\n/>\n```\n\n**Props**:\n- `label`: string (optional)\n- `type`: string (default: 'text')\n- `error`: string (optional)\n- `icon`: React.ReactNode (optional)\n- `helperText`: string (optional)\n- Standard HTML input attributes\n\n### FormSelect\nDropdown select component.\n\n```typescript\n<FormSelect\n  label=\"Account Type\"\n  options={[\n    { value: 'checking', label: 'Checking Account' },\n    { value: 'savings', label: 'Savings Account' },\n    { value: 'money-market', label: 'Money Market' },\n  ]}\n  error={errors.accountType}\n  helperText=\"Select your account type\"\n/>\n```\n\n**Props**:\n- `label`: string (optional)\n- `options`: Array<{ value: string; label: string }>\n- `error`: string (optional)\n- `helperText`: string (optional)\n- Standard HTML select attributes\n\n### FormTextarea\nMulti-line text input.\n\n```typescript\n<FormTextarea\n  label=\"Message\"\n  placeholder=\"Enter your message...\"\n  rows={4}\n  error={errors.message}\n  helperText=\"Maximum 500 characters\"\n/>\n```\n\n**Props**:\n- `label`: string (optional)\n- `error`: string (optional)\n- `helperText`: string (optional)\n- Standard HTML textarea attributes\n\n## 🎨 Styling Classes\n\n### Colors\n```typescript\n// Text colors\n'text-gray-900 dark:text-white'      // Primary text\n'text-gray-500 dark:text-gray-400'   // Secondary text\n'text-blue-600 dark:text-blue-400'   // Links/Primary\n'text-green-600 dark:text-green-400' // Success\n'text-red-600 dark:text-red-400'     // Error\n'text-yellow-600 dark:text-yellow-400' // Warning\n\n// Background colors\n'bg-white dark:bg-gray-800'          // Card/Modal\n'bg-gray-50 dark:bg-gray-900'        // Section\n'bg-blue-100 dark:bg-blue-900'       // Info\n'bg-green-100 dark:bg-green-900'     // Success\n'bg-red-100 dark:bg-red-900'         // Error\n```\n\n### Borders\n```typescript\n'border border-gray-200 dark:border-gray-700'        // Default\n'border-2 border-blue-500'                           // Accent\n'border-red-300 dark:border-red-700'                 // Error\n'rounded-lg'                                         // Rounded corners\n'rounded-full'                                       // Pill shape\n```\n\n### Spacing\n```typescript\n'p-4'       // Padding all\n'px-4 py-2' // Horizontal/Vertical\n'gap-4'     // Gap between flex items\n'space-y-4' // Vertical spacing between children\n```\n\n## 📊 Page Components\n\n### DashboardOverview\nHome page with balance, stats, and recent activity.\n\n```typescript\nimport { DashboardOverview } from '@/components/dashboard';\n\n<DashboardOverview />\n```\n\n### TransactionsPage\nTransaction history with filters.\n\n```typescript\nimport { TransactionsPage } from '@/components/dashboard';\n\n<TransactionsPage />\n```\n\n### TransfersPage\nMoney transfer interface.\n\n```typescript\nimport { TransfersPage } from '@/components/dashboard';\n\n<TransfersPage />\n```\n\n### VirtualCardsPage\nCard management interface.\n\n```typescript\nimport { VirtualCardsPage } from '@/components/dashboard';\n\n<VirtualCardsPage />\n```\n\n## 🔗 Icons Reference\n\nUse Lucide React icons:\n\n```typescript\nimport {\n  Home,\n  Send,\n  CreditCard,\n  Eye,\n  EyeOff,\n  Check,\n  X,\n  AlertCircle,\n  ArrowUpRight,\n  ArrowDownLeft,\n  Menu,\n  Settings,\n  Bell,\n  User,\n  LogOut,\n  ChevronDown,\n  Filter,\n  Search,\n  Download,\n  Plus,\n  Edit,\n  Trash2,\n  Lock,\n  Unlock,\n} from 'lucide-react';\n```\n\n## 🎭 Theme Support\n\n### Light Mode (default)\n```typescript\n// Component automatically uses light theme\n<StatusBadge status=\"success\" label=\"Active\" />\n```\n\n### Dark Mode\n```typescript\n// Add dark:* classes in Tailwind config\n// Enable dark mode in your root element:\n<html className=\"dark\">\n  <body>\n    <DashboardMain />\n  </body>\n</html>\n```\n\n## 🔒 Form Validation\n\n```typescript\nconst [errors, setErrors] = useState<Record<string, string>>({});\n\nconst handleSubmit = (e: React.FormEvent) => {\n  e.preventDefault();\n  const newErrors: Record<string, string> = {};\n  \n  // Validate\n  if (!email) newErrors.email = 'Email is required';\n  if (!password) newErrors.password = 'Password is required';\n  \n  setErrors(newErrors);\n  if (Object.keys(newErrors).length === 0) {\n    // Submit form\n  }\n};\n\nreturn (\n  <form onSubmit={handleSubmit}>\n    <FormInput\n      label=\"Email\"\n      type=\"email\"\n      error={errors.email}\n      placeholder=\"your@email.com\"\n    />\n    <FormInput\n      label=\"Password\"\n      type=\"password\"\n      error={errors.password}\n      placeholder=\"••••••••\"\n    />\n  </form>\n);\n```\n\n## 🎯 Responsive Breakpoints\n\n```typescript\n'block md:hidden'     // Mobile only\n'hidden md:block'     // Desktop only\n'grid-cols-1 md:grid-cols-2 lg:grid-cols-3' // Responsive grid\n'w-full md:w-1/2 lg:w-1/3' // Responsive width\n```\n\n## 💡 Pro Tips\n\n1. **Always use TypeScript interfaces** for component props\n2. **Test dark mode** by adding `dark` class to parent\n3. **Use semantic HTML** for accessibility\n4. **Keep components pure** and avoid side effects\n5. **Memoize expensive components** with `React.memo`\n6. **Use proper loading states** for async operations\n7. **Provide helpful error messages** to users\n8. **Test on mobile** before shipping\n\n## 🐛 Common Issues\n\n### Dark mode not working\n- Ensure Tailwind CSS config has `darkMode: 'class'`\n- Add `dark` class to parent HTML element\n\n### Icons not showing\n- Import from `lucide-react`, not other libraries\n- Check icon name spelling\n\n### Form validation not updating\n- Update state in onChange handler\n- Pass error state to FormInput component\n\n### Responsive layout broken\n- Use `md:`, `lg:`, `xl:` prefixes correctly\n- Test with different screen sizes\n\n## 📚 More Resources\n\n- Full documentation: See `README.md`\n- Design system: See `DESIGN_SYSTEM.md`\n- Project status: See `DASHBOARD_COMPLETION.md`\n\n---\n\n**Need help?** Check the comprehensive README.md for detailed documentation.\n"